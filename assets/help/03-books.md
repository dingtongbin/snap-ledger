# 账本

多账本分组、切换与归属规则。

- 明细 / 统计页顶部有账本切换 chip，默认显示「全部账本」
- 切换到具体账本后，页面只显示该账本的账单与统计
- 新建账单时：选中的账本就是归属；未选则记入「默认账本」
- 记账弹层内也能临时改本笔账单的归属账本

## 管理账本

设置 → 账本管理：

- 可新建 / 改名 / 删除账本
- 默认账本不可删除
- 有账单的账本拒绝删除，需要先移走或删除其中的账单
